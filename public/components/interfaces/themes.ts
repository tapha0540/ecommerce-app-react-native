export default interface Theme {
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
  iconColor: string;
  backgroundColor: string;
  fontFamily: string;
  errorColor: string;
  successColor: string;
  cardBorderWidth: number;
}
